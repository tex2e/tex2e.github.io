'use strict';

$(document).ready(function() {
  // langs-plot.js
  plotGraph();

  // lib/skrollr.js
  var s = skrollr.init({
    smoothScrolling: true, //false,
    mobileDeceleration: 0.004, //1,
  });
});
